{
  "name": "msa-grader",
  "version": "1.0.0",
  "description": "MSA Grader – Google Apps Script backend (colocated in CleverPlatform monorepo)",
  "private": true,
  "scripts": {
    "reauth": "clasp logout && clasp login --creds ./clasp-oauth.json && clasp show-authorized-user",
    "gas:status": "clasp status",
    "gas:pull": "clasp pull",
    "push": "clasp push",
    "gas:push": "npm run push",
    "deploy": "clasp deploy",
    "sync": "git pull && npm run gas:pull",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "tests/**/*.test.js"
    ]
  },
  "devDependencies": {
    "@google/clasp": "^3.2.0",
    "jest": "^29.0.0"
  }
}
